# المكتبات الأساسية لتطبيق Chat Me
# نسخة Python المطلوبة: 3.7 أو أحدث

# لا توجد مكتبات خارجية مطلوبة!
# التطبيق يستخدم فقط المكتبات المضمنة في Python:

# tkinter - لواجهة المستخدم الرسومية
# sqlite3 - لقاعدة البيانات
# socket - للاتصال بين العميل والخادم
# threading - للمعالجة المتوازية
# json - لتبادل البيانات
# hashlib - لتشفير كلمات المرور
# datetime - للتعامل مع الوقت والتواريخ
# queue - لإدارة قوائم الانتظار
# os - للتعامل مع نظام الملفات
# sys - للتعامل مع النظام

# ملاحظة: كل هذه المكتبات مثبتة مسبقاً مع Python
# لا تحتاج لتثبيت أي شيء إضافي!
